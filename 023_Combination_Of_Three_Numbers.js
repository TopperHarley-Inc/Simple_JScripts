// 023. Combination of three numbers
// The combining of three numbers into one

function numberCombination(x, y, z) {
    return x + "" + y + "" + z;
}

let combinationResult = numberCombination(1, 5, 3);
alert(combinationResult);