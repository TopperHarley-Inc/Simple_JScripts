// 003. A square area
// A simple script, that counts a square area using a side length

let sideLength = prompt('How long a side of your square?');
let squareArea = sideLength ^ 2;

alert(`The square area is ${square_area} cm2.`);