// 004. A square area
// A simple script, that counts a circle area using it`s radius

let radius = prompt('Please, enter a circle radius in cm.');

const pi = 3.14;
let circleArea = radius * radius * pi;

alert(`The circle area is ${circleArea} cm2.`);