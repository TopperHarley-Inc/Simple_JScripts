// 005. A square area
// A simple script, that counts a moving speed

let distance = prompt('Please, enter a distance in km.');
let time = prompt('Please, enter a time, needed to reach the destanation.');

let movingSpeed = distance / time;

alert(`Moving speed will be no less than ${movingSpeed} km/h.`);